"use client";

import { IconBrandTabler } from "@tabler/icons-react";

export function LogoIcon({ className }: { className?: string }) {
  return (
    <IconBrandTabler className={className} />
  );
}